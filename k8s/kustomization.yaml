namespace: default
resources:
  - mariadb.yaml
  - backend.yaml
  #  - ingress.yaml
  - nginx.yaml
  - redis.yaml
configMapGenerator:
  - name: init-db
    files:
      - configs/init.sql
  - name: nginx
    files:
      - configs/default.conf
